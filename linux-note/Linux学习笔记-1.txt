目录
第1章 Linux简介
第2章 Linux系统安装
第3章 命令基本格式即文件处理命令
第4章 文件搜索命令
第5章 帮助命令
第6章 压缩命令
第7章 关机与重启命令
第8章 其他常用命令
第9章 Shell基础


-------------------------------------------------------
第1章 Linux简介
1-1 Linux简介
1-2 开源软件简介
1-3 练习题
1-4 Linux应用领域
1-5 练习题
1-6 Linux学习方法
1-7 Linux与Windows的不同
1-8 字符界面的优势
1-9 练习题

1-1 Linux简介
    Linux内核版本（主版本.次版本.末版本）
    Linux发行版本（基于内核版本添加新的功能，软件本身免费，收取服务器）
        RedHat, CentOS, Debain, 红旗

1-2 开源软件简介
    Apache, Nginx, MySQL, php, Python, mongodb, Ruby, Sphinx, samba
    使用的自由，绝大多数开源软件免费
    研究的自由，可以获得软件源代码
    散布及改良的自由，可以自由传播、改良甚至销售

1-3 练习题

1-4 Linux应用领域
    -基于Linux的企业服务器
    -Linux嵌入式应用

1-5 练习题

1-6 Linux学习方法
应该如何提问？
    先要尝试自己解决：帮助、文档、示例
    提问的智慧：问题详尽（具体的知识点）、贴图

1-7 Linux与Windows的不同
-Linux严格区分大小写
-Linux中所有内容以文件形式保存，包括硬件、用户等
-Linux不靠扩展名区分文件类型（windows的扩展名对应一种应用），靠文件的权限区分，但是有约定俗成的扩展名以方便的管理员区分文件
     压缩包：*.gz、 *.bz2、 *.tar.bz2、 *.tgz等
     二进制软件包： .rpm
     网页文件：.html, .php
     脚本文件：.sh
     配置文件：.conf
-Windows下的程序不能直接在Linux中安装和运行


1-8 字符界面的优势
    优势1：字符界面占用的系统资源更少
    优势2：字符界面减少了出错、被攻击的可能性（启动的进程更少，服务器系统的安全性是优先的，应用系统的易用性是优先的）

1-9 练习题


-------------------------------------------------------
第2章 Linux系统安装
2-1 虚拟机的安装
2-2 虚拟机的使用
2-3 系统分区之分区与格式化
2-4 分区之分区设备文件名誉挂载
2-5 练习题
2-6 Linux系统安装
2-7 练习题
2-8 XShell的安装和使用


2-1 虚拟机的安装
1. VMware简介
VMware是一个虚拟PC的软件，可以在现有的操作系统上虚拟出一个新的硬件环境，相当于模拟出一台新的PC，
依次来实现在一台机器上真正同时运行两个独立的操作系统。
使用VMware8，占用空间较小。

VMware主要特点：
    -不需要分区或重新开机就能在同一台PC上使用两种以上的操作系统
    -本机系统可以与虚拟机系统网络通信
    -可以设定并且随时修改虚拟机操作系统的硬件环境
    虚拟机有快照功能，如果系统崩溃了，系统可以恢复到快照时的状态。

2、建议的VMware配置
    CPU：建议主频为1GHz以上
    内存：建议1GB以上
    硬盘：建议分区空闲空间8G以上

2-2 虚拟机的使用
    新建虚拟机
    虚拟机硬件设置

2-3 系统分区之分区与格式化
1. 磁盘分区
    磁盘分区是使用分区编辑器（partition editor）在磁盘上划分几个逻辑部分。（不是物理上的划分，只是概念上的分区）
    碟片一旦划分成数个分区（Partition），不同类的目录与文件可以存储进不同的分区。

    分区类型
        主分区：最多只能有4个。
        扩展分区：
            做多只能有1个。
            主分区加扩展分区最多有4个。
            扩展分区不能写入数据，只能包含逻辑分区。
        逻辑分区

2. 格式化
    格式化（高级格式化）又称逻辑格式化，它是指根据用户选定的文件系统（如FAT16、FAT32、NTFS、EXT2、EXT3、EXT4等），
    在磁盘的特定区域写入特定数据，在分区中划出一片用于存放文件分配表、目录表等用于文件管理的磁盘空间。

    NTFS支持更大的空间和更大的单个文件大小。
    格式化就是写入文件系统，它包括两部分，一是把磁盘分为等大小的数据块；二是在分区中建立二维表格，记录每个文件的ID号、修改时间和权限。
    默认数据块（block）的大小是4kB。
    格式化会将数据清空，这只是它的效果，它的目的是写入文件系统。

2-4 分区之分区设备文件名与挂载
3. 硬件设备文件名
    硬件                      设备文件名
    IDE硬盘                   /dev/hd[a-d]
    SCSI/SATA/USB硬盘         /dev/sd[a-p]
    光驱                      /dev/cdrom或/dev/hdc
    软盘                      /dev/fd[0-1]
    打印机（25针）             /dev/lp[0-2]
    打印机（USB）              /dev/usb/lp[0-15]
    鼠标                      /dev/mouse

    /表示根目录
    dev目录下存放的硬件设备文件

    分区设备文件名
        设备文件名
            /dev/hda1（IDE硬盘接口）
            /dev/sda1（SCSI硬盘接口、SATA硬盘接口）

    分区 --> 格式化 --> 分区设备名 --> 挂载点（相当于Windows的盘符）
    1、2、3、4四个分区号只能给主分区，逻辑分区是从5开始的。

4、挂载
    必须挂载
        /（根分区）
        swap分区（交换分区，内存2倍，不超过2GB）
    推荐分区
        /boot（启动分区，200MB）

总结：
分区：把大硬盘分为小的逻辑分区
格式化：写入文件系统
分区设备文件名：给每个分区定义设备文件名
挂载：给每个分区分配挂载点

2-5 练习题

2-6 Linux系统安装


2-7 练习题
2-8 XShell的安装和使用

-------------------------------------------------------
第3章 命令基本格式即文件处理命令

命令分类：
    -命令基本格式
    -文件处理命令
    -文件搜索命令
    -帮助命令
    -压缩与解压缩命令
    -关机和重启命令
    -其他常用命令

Linux命令超过3000个，最常用的命令约200个，将命令分类便于记忆。
本教程将学习70个命令。

------------------------------
3-1 Linux命令基本格式
[root@localhost ~] #
    root            当前登录用户
    localhost       主机名
    ~               当前所在目录（家目录），每个用户都有自己的家目录，root-/root, user1-/home/user1
    #               超级用户的提示符
                    普通用户的提示符是$

pwd -- 当前目录
cd -- 切换目录

命令格式
    命令 [选项] [参数]
注意： 个别命令使用不遵循此格式
      当有多个选项时，可以写在一起，简化选项与完整选项，选项可以调整命令的功能
        -a 等于 --all，并不是所有命令都支持 -a 选项


查询目录中内容：ls
ls [选项] [文件或目录]
选项：
    -a 显示所有文件，包括隐藏文件（all）
    -l 显示详细信息 （long）
    -d 查看目录属性
    -h 人性化显示文件大小
    -i 显示inode（文件的id号）

[root@localhost bin]# ls -l
dr-xr-xr-x.   2 root root  4096 10月  5 04:55 bin
d 文件类型（- 文件， d 目录， l 软链接文件）
    Linux一共有7种文件，还包括块设备文件、字符设备文件、套接字文件和管道文件，通过权限的第一个字符区分
r-x，u所有者的权限
r-x，g所属组的权限（相同身份或者相同权限的人分为一组）
r-x，o其他人的权限
r-read-4
w-write-2
x-execute-1

dr-xr-xr-x（权限）.   2（引用计数） root（所有者） root（所属组）  4096（文件大小） 10月  5 04:55（最后一个修改时间） bin（文件名）
4096-文件大小， ls -lh（h代表human，为了人们更好的理解数据的含义，只要包含文件大小的命令均可以使用-h命令）

[选项]，是为调整命令的功能的。
[root@localhost bin]# ls -lh cat
-rwxr-xr-x. 1 root root 44K 11月 22 2013 cat
[root@localhost bin]# ls（命令） -lh（选项） cat（参数）

Linux中以.开头的文件是隐藏文件，一般是系统文件，可以提示用户不要轻易修改它。

[root@localhost bin]# ls -i


ll 是 ls -l 命令的别名


------------------------------
3-2 Linux目录处理命令
目录处理命令（windows中的文件夹，是用来保存文件的）
文件处理命令（文件，是用来保存数据的）
链接命令

目录处理命令
创建目录
    mkdir -p 目录名
        -p 递归创建
    [root@localhost home]# mkdir -p liyazhoutemp/dir

切换目录
    cd 目录（change directory）

    简化操作
        cd ~或cd    进入当前用户的家目录
        cd -        进入上次目录
        cd ..       进入上一级目录
        cd .        进入当前目录

快捷键：Ctrl + L    清屏，clear


相对路径：参照当前所在目录，进行查找，如 cd ../usr/local/src/
绝对路径：从根目录开始指定，一级一级递归查找。在任何目录下，都能进入指定位置，如 cd /home/
目录补全，双击或者单击Tab键，加快书写命令，而且可以确保命令正确性


查询所在目录位置：pwd
    pwd, print working directory，打印当前目录


删除空目录：rmdir（很少使用）
    rmdir 目录名
        remove empty directories


删除文件或目录：rm
    rm -rf 文件或目录
        remove
        选项：
            -r  删除目录（删除目录时使用，删除文件时也可以使用）
            -f  强制（不用询问是否确定删除，直接删除）

            rm -rf / 自杀式命令，将会删除绝大数文件，Linux的root权限非常大


复制命令：cp
    cp [选项] [原文件或目录] [目标目录]
        copy
        选项：
            -r  复制目录
            -p  连带文件属性复制（创建时间等）
            -d  若原文件是链接文件，则复制链接属性
            -a  相当于-pdr，目标文件和原文件一模一样

剪切或改名命令：mv
    mv [原文件或目录] [目标目录]
        move
        如果原文件和目标文件在同一个目录，就是改名功能
        如果原文件和目标文件不在同一个目录，就是剪切


------------------------------
3-3 Linux中常见目录作用

[root@localhost /]# ls
bin   cgroup  etc   lib         media  mnt  opt   root  selinux  sys  usr
boot  dev     home  lost+found  misc   net  proc  sbin  srv      tmp  var

/       根目录
/root   超级用户的家目录
根目录下的bin和sbin，usr目录下的bin和sbin，这四个目录都是用来保存系统命令的。其中sbin中命令只用root用户才可以使用。
/usr    系统软件资源目录
/boot   启动目录
/dev    设备目录（硬件）
/etc    系统配置文件目录
/lib
/media、/mnt、/misc（外接目录，可以挂在u盘光盘等）
/tmp    临时目录
/var    系统相关文档内容

可以在家目录root或home，以及tmp目录下随便存放内容。


------------------------------
3-4 Linux链接命令
链接命令：ln

ln -s [原文件] [目标文件]
    link
    功能描述：生成链接文件
    选项：-s   创建软链接（soft）

硬链接与软链接：
硬链接特征：（一个真实文件的多个别名）
1 拥有相同的i节点和存储block块，可以看做是同一个文件
2 可通过i节点识别
3 不能跨分区（可以把硬链接看做一个教室的两个门，这个教室不能同时在两栋楼中）
4 不能针对目录使用

软链接特征：（推荐使用软链接）
1 类似windows快捷方式
2 软链接拥有自己的I节点和Block块，但是数据块中只保存原文件的文件名和I节点号，并没有实际的文件数据
3 lrwxrwxrwx l 软链接
    软链接文件权限都为 rwxrwxrwx
4 修改任意文件，另一个都改变
5 删除原文件，软链接不能使用

创建软链接时，原文件一定要写绝对路径，除非和目标文件在同一个目录下



-------------------------------------------------------
第4章 文件搜索命令
4-1 文件搜索命令locate
4-2 命令搜索命令
4-3 find命令
4-4 grep命令

文件搜索命令 locate
命令搜索命令 whereis 与 which
文件搜索命令 find
字符串搜索命令 grep
find命令与grep命令的区别

------------------------------
4-1 文件搜索命令locate
locate命令格式
locate 文件名
    在后台数据库中按文件名搜索，搜索速度更快
    /var/lib/mlocate，是locate命令所搜索的后台数据库，不用遍历整个文件系统
    updatedb，强制更新数据库，默认一天更新一次

[root@localhost home]# locate liyazhou
locate: can not stat () `/var/lib/mlocate/mlocate.db': 没有那个文件或目录
[root@localhost home]# updatedb
[root@localhost home]# locate liyazhou

优点，搜索速度快
缺点，只能按照文件名称搜索，功能单一，与find互补
一般情况下，按文件名搜索情况较多，所以locate较常用


------------------------------
4-2 命令搜索命令
命令搜索命令 whereis
whereis 命令名
    搜索命令所在路径及帮助文档所在位置
    选项：
        -b：只查找可执行文件
        -m：只查找帮助文件

whereis 命令
whatis 命令
whoami

whereis和which区别
whereis可以看到命令所在的位置和帮助文档
which 可以查看命令所在位置，如果有别名也将显示

cd 是shell的内置命令（自带），它没有执行文件
whereis可以查看外部安装的命令，不可以查看shell内置的命令


ls 命令，可以不带路径，因为有$PATH，类似于windows的环境变量
PATH环境变量
PATH环境变量：定义的是系统搜索命令的路径
[root@localhost ~]# echo $PATH
/usr/lib/qt-3.3/bin:/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin:/root/bin

whereis和which是依赖$PATH路径的


------------------------------
4-3 find命令
文件搜索命令find
功能强大，参数较多，学习起来复杂，此处仅仅学习常用的操作

find [搜索范围] [搜索条件]
    find / -name install.log
    避免大范围搜索，会非常耗费系统资源
    find是在系统中搜索符合条件的文件名。如果需要匹配，使用通配符匹配，通配符是完全匹配。

[root@localhost ~]# find / -name liyazhoutemp
/home/liyazhoutemp

[root@localhost ~]# find / -name liyazhou*
/home/liyazhoutemp

Linux中的通配符
    *   匹配任意内容
    ?   匹配任意一个字符
    []  匹配中括号内任意的一个字符

不区分大小写
find /root -iname install.log

按照所有者搜索
find /root -user root

查找没有所有者的文件
find /root -nouser
没有所有的文件，一般是垃圾文件
一是，内核产生的文件可能没有所有者，比如在/sys
二是，外来的文件，可能没有所有者
除了两者之外，没有所有者的文件可以看做是垃圾文件

查找10天前修改的文件（处理日志文件常用）
find /var/log/ -mtime +10
    -10     10内修改的文件
     10     10天当前修改的文件
    +10     10天前修改的文件

    atime   文件访问时间
    ctime   改变文件属性
    mtime   修改文件内容

查找文件大小是25kB的文件
find . -size 25k
    -25k    小于25kB的文件
    25k     等于25kB的文件
    +25     大于25kB的文件
单位
KB——k（小写）
MB——M（大写）

查找inode是262422的文件
find . -inum 262422
    跟 ls -i 配合使用
    ls -i 查看inode
    find dir -inum inodeid 已知文件编号查文件


查找/etc/目录下，大于20KB并且小于50KB的文件
find /etc -size +20k -a -size -50k
    -a and 逻辑与，两个调价都满足
    -o or  逻辑或，两个条件满足一个即可


查找/etc/目录下，大于20KB并且小于50KB的文件，并显示详细信息
find /etc -size +20k -a -size -50k -exec ls -lh {} \;
    对搜索结果执行操作 -exec/-ok 命令 {} \;


------------------------------
4-4 grep命令
搜索字符串命令 grep

在文件当中匹配符合条件的字符串
grep [选项] 字符串 文件名
    选项：
        -i 忽略大小写
        -v 排除指定字符串

[root@localhost ~]# grep "size" anaconda-ks.cfg

find命令和grep命令的区别
find命令，在系统当中搜索符合条件的文件名，如果需要匹配，使用通配符匹配，通配符是完全匹配。
grep命令，在文件当中搜索符合条件的字符串，如果需要匹配，使用正则表达式进行匹配，正则表达式时包含匹配。


-------------------------------------------------------
第5章 帮助命令
5-1 帮助命令man
5-2 其他帮助命令


------------------------------
5-1 帮助命令man
获取命令的帮助
man 命令
    如查看ls的帮助，man ls


man的级别
1，查看命令的帮助
2，查看可被内核调用的函数的帮助
3，查看函数和函数库的帮助
4，查看特殊文件的帮助（主要是/dev目录下的文件）
5，查看配置文件的帮助
6，查看游戏的帮助
7，查看其它杂项的帮助
8，查看系统管理员可用命令的帮助
9，查看和内核相关文件的帮助

查看命令拥有哪个级别的帮助
man -f 命令
相当于 whatis 命令

举例：
[root@localhost ~]# man -5 passwd
man -4 null
man -8 ifconfig


查看和命令相关的所有帮助
man -k 命令
相当于 apropos 命令

例如 apropos passwd

------------------------------
5-2 其他帮助命令

选项帮助
获取命令选项的帮助
命令 --help
    如 ls --help

shell内部命令帮助
获取shell内部命令的帮助
help shell 内部命令
    确定是否是shell内部命令 whereis cd
    获取内部命令帮助  help cd

shell（壳，内核的壳），用户和计算机交互的接口
cd是shell的内置命令


详细命令帮助info
info 命令
    -回车：    进入子帮助页面（带有*号标记）
    -u：       进入上层页面
    -n：       进入下一个帮助小节
    -p：       进入上一个帮助小节
    -q：       退出


小结：
man 和 ls --help更为好用

-------------------------------------------------------
第6章 压缩命令
常用压缩格式：.zip     .gz      .bz2
常用压缩格式：         .tar.gz  .tar.bz2（最为常用的压缩命令，先打包后压缩）

按压缩格式记忆压缩命令：

.zip格式压缩
    压缩文件：zip 压缩文件名 原文件
    压缩目录：zip -r 压缩文件名 原文件
[root@localhost tmp]# zip newdir.zip newdir
压缩后的文件不一定就比原文件小，因为压缩文件需要包含压缩格式内容
zip格式的压缩包，在linux和windows下是通用的

创建目录和文件
mkdir newdir
touch newfile

.zip格式解压缩
    解压缩.zip文件：unzip 压缩文件


.gz格式压缩
    gzip 源文件
    压缩为.gz格式的压缩文件，原文件会消失

    gzip -c 源文件 > 压缩文件
    压缩为.gz格式，源文件保留
        例如：gzip -c cangls > cangls.gz

    gzip -r 目录
    压缩目录下所有的子文件，但是不能压缩目录

    rm -rf *.zip

.gz 格式解压缩
    gzip -d 压缩文件
    解压缩文件

    gunzip 压缩文件
    解压缩文件


.bz2格式压缩
    bzip2 源文件
    压缩为.bz2格式，不保留源文件

    bzip2 -k 源文件
    压缩之后保留源文件

    注意：bzip2命令不能压缩目录

.bz2格式解压缩
    bzip2 -d 压缩文件
    解压缩，-k保留压缩文件

    bunzip2 压缩文件
    解压缩，-k保留压缩文件


tar.gz, tar.bz2压缩格式
    解决gz,bz2不能压缩目录的问题

打包命令tar
    tar -cvf 打包文件名 源文件
        选项：
            -c：打包
            -v：显示过程
            -f：指定打包后的文件名
         例如：
            tar -cvf targetfile.tar targetfile


解打包命令tar
    tar -xvf 打包文件名
        选项：
            -x：打包
            -v：显示过程
            -f：指定打包后的文件名
         例如：
            tar -xvf targetfile.tar


tar打包之后再使用gz或bz2解压目录

.tar.gz压缩格式
    其实.tar.gz格式是先打包为.tar格式，再压缩为.gz格式

    tar -zcvf 压缩包名.tar.gz 源文件
        选项：
            -z：压缩为.tar.gz格式

    tar -zxvf 压缩包名.tar.gz
         选项：
            -x：解压缩.tar.gz格式



.tar.bz2压缩格式
    其实.tar.bz2格式是先打包为.tar格式，再压缩为.bz2格式

    tar -jcvf 压缩包名.tar.bz2 源文件
        选项：
            -z：压缩为.tar.bz2格式

    tar -jxvf 压缩包名.tar.bz2
         选项：
            -x：解压缩.tar.bz2格式

-------------------------------------------------------
第7章 关机与重启命令

1. shutdown命令
shutdown [选项] 时间
    选项：
        -c：取消前一个关机命令，cancel
        -h：关机，（服务器不用远程关机，可以远程重启）
        -r：重启，restart

2. 其他关机命令
    halt
    poweroff
    init 0（调用系统的运行级别）

3. 其他重启命令
    reboot
    init 6（调用系统的运行级别）

4. 系统运行级别
    0   关机
    1   单用户
    2   不完全多用户，不含NFS服务
    3   完全多用户
    4   未分配
    5   图形界面
    6   重启


修改系统默认运行级别：
cat /etc/inittab
id:3:initdefault

查询系统运行级别：
runlevel

5. 退出登录命令
logout


-------------------------------------------------------
第8章 其他常用命令
8-1 挂载命令
8-2 用户登录查看命令与用户交互命令

------------------------------
8-1 挂载命令
可以理解为Windows中的分配盘符，Linux中称作挂载点

1. 查询与自动挂载
    mount
    查询系统中已经挂载的设备

    mount -a
    依据配置文件/etc/fstab的内容，自动挂载，-a，all

2. 挂载命令格式
    mount [-t 文件系统] [-o 特殊选项] 设备文件名 挂载点
    选项：
        -t 文件系统：加入文件系统类型来指定挂载的类型，可以ext3、ext4、iso9660等文件系统
        -o 特殊选项：可以指定挂载的额外选项

3. 挂载光盘
    mkdir /mnt/cdrom/
    建立挂载点

    mount -t iso9660 /dev/cdrom/mnt/cdrom/
    挂载光盘

4. 卸载命令
    umount 设备文件名或挂载点
    umount /mnt/cdrom

5. 挂载U盘
    fdisk -l
    查看U盘设备文件名

    mount -t vfat /dev/sdb1/mnt/usb/
    注意：Linux默认是不支持NTFS文件系统的


------------------------------
8-2 用户登录查看命令

查看登录用户信息
w 用户名

who 用户名
    命令输出：
        - 用户名
        - 登录终端
        - 登录时间（登录来源IP地址）

last
    last 命令默认是读取 /var/log/wtmp文件数据
        命令输出：
            -用户名
            -登录终端
            -登录IP
            -登录时间
            -退出时间（在线时间）

lastlog
    lastlog命令默认是读取/var/log/lastlog文件内容
    命令输出：
        -用户名
        -登录终端
        -登录IP
        -最后一次登录时间

-------------------------------------------------------
第9章 Shell基础

一、Shell概述
二、脚本执行方式
三、Bash的基本功能


9-1 Shell概述
9-2 脚本执行方式
9-3 别名与快捷键
9-4 历史命令
9-5 输出重定向
9-6 管道符
9-7 通配符


------------------------------
9-1 Shell概述

1. Shell是什么
Shell 是一个命令行解释器，它为用户提供了一个向Linux内核发送请求以便运行程序的界面系统级程序，
用户可以用Shell来启动、挂起、停止甚至编写一些程序。

Shell还是一个功能相当强大的编程语言，易编写，易调试，灵活性较强。
Shell是解释执行的脚本语言，在Shell中可以直接调用Linux系统命令。


2.Shell的分类
Bourne Shell：从1979起Unix就开始使用Bourne Shell, Bourne Shell的主文件名为sh。（不支持历史命令）
C Shell：C Shell主要在BSD版的Unix系统中使用，其语法和C语言相类似而得名。

Shell的两种主要语法类型有Bourne和C，这两种语法彼此不兼容。
Bourne家族主要包括sh、ksh、Bash、psh、zsh；
C家族主要包括：csh、tcsh。

Bash：Bash与sh兼容，现在使用的Linux就是使用Bash作为用户的基本Shell。
Linux中的标准Shell是Bash。

3. Linux支持的Shell
echo $SHELL
/etc/shells


------------------------------
9-2 脚本执行方式

1. echo

2. 第一个脚本
#!/bin/bash
#The first program
（第一行不可省略）

3. 脚本执行
(1)赋予执行权限，直接运行（习惯的用法）
    chmod 755 hello.sh
    ./hello.sh
(2)通过Bash调用执行脚本（不符合习惯）
    bash hello.sh


------------------------------
9-3 别名与快捷键

Bash的基本功能
命令别名与快捷键
历史命令
输出重定向
多命令顺序执行
Shell中特殊符号


什么是别名
命令别名 == 人的小名

查看与设定别名
    alias
    查看系统中所有的命令别名

    alias 别名 = '原命令'
    设定命令别名

别名永久生效与删除别名
    vi ~/.bashrc
    写入环境变量配置文件

    unalias 别名
    删除别名

命令生效顺序
    第一顺位执行用绝对路径或相对路径执行的命令。
    第二顺位执行别名。
    第三顺位执行Bash的内部命令。
    第四顺位执行按照$PATH环境变量定义的目录查找顺序找到的第一个命令。

常用快捷键：
Ctrl + c    强制终止当前命令
Ctrl + l    清屏，clear
Ctrl + a    光标移动到命令行首
Ctrl + e    光标移动到命令行尾
Ctrl + u    从光标所在位置删除到行首
Ctrl + z    把命令放入后台
Ctrl + r    在历史命令中搜索


------------------------------
9-4 历史命令

1. 历史命令
history [选项] [历史命令保存文件]
    选项：
        -c：清空历史命令
        -w：把缓存中的历史命令写入历史命令保存文件~/.bash_history

历史命令默认会保存1000条，可以在环境变量配置文件/etc/profile中进行修改

历史命令的调用
    使用上、下箭头调用以前的历史命令
    使用"!n"重复执行第n条历史命令
    使用"!!"重复执行上一条命令
    使用"!字串"重复执行最后一条以该字串开头的命令

2. 命令与文件不全
在Bash中，命令与文件补全是非常方便与常用的功能，我们只要在输入命令或文件时，
按Tab键就会自动进行补全（也可以用来验证命令是否写错）


------------------------------
9-5 输出重定向

1. 标准输入输出

设备      设备文件名       文件描述符       类型
键盘      /dev/stdin      0               标准输入
显示器    /dev/stdout     1               标准输出
显示器    /dev/stderr     2               标准错误输出


2. 输出重定向

标准输出重定向：
命令 > 文件，以覆盖的方式，把命令的正确输出输出到指定的文件或设备当中
命令 >> 文件，以追加的方式，把命令的正确输出输出到指定的文件或设备当中

标准错误输出重定向
错误命令 2>文件（注意，此处没有空格），以覆盖的方式，把命令的错误输出输出到指定的文件或设备当中
错误命令 2>>文件（注意，此处没有空格），以追加的方式，把命令的错误输出输出到指定的文件或设备当中

[root@localhost ~]# ll -h > /root/tmp/redirectfile.zhou
[root@localhost ~]# cat /root/tmp/redirectfile.zhou


正确输出和错误输出同时保存（最为常用）
命令 > 文件2>&1，以覆盖的方式，把正确输出和错误输出都保存到同一个文件当中
命令 >> 文件 2>&1，以追加的方式，（掌握）
命令 &>文件，以覆盖的方式，
命令 &>>文件，以追加的方式，（掌握）
命令>>文件1 2>>文件2，把正确的输出追加到文件1中，把错误的输出追加到文件2中（掌握）



3. 输入重定向（相对于输出重定向，输出重定向较少使用）
wc [选项] [文件名]
    选项：
        -c：统计字节数
        -w：统计单词书
        -l：统计行数

命令 < 文件把文件作为命令的输入
命令 << 标识符
标识符把标识符之间内容作为命令的输入


------------------------------
9-6 管道符（非常常用）

1. 多命令顺序执行
符号 ;
格式 命令1; 命令2
作用 多个命令顺序执行，命令之间没有任何逻辑关系

符号 &&
格式 命令1&&命令2
作用 逻辑与，短路与操作

符号 ||
格式 命令1||命令2
作用 逻辑或，短路或

2. 管道符
命令格式：命令1 | 命令2
作用：命令1的正确输出作为命令2的操作对象
注意，命令2一定能够操作命令1的输出
[root@localhost ~]# ls -lh | more
more 分屏查看文件

[root@localhost ~]# netstat -an | grep ESTABLISHED
tcp        0      0 192.168.13.215:22           192.168.13.2:54033          ESTABLISHED

[root@localhost ~]# netstat -an | grep ESTABLISHED | wc -l
1

wc -l 文件，统计行号

------------------------------
9-7 通配符

1. 通配符
?       匹配一个任意字符
*       匹配0个或任意多个任意字符，也就是可以匹配任何内容
[]      匹配中括号中任意一个字符。例如：[abc]代表一定匹配其中的一个字符
[-]     匹配括号中任意一个字符，-代表一个范围。例如[a-z]代表匹配一个小写字母。
[^]     逻辑非，表示匹配不是中括号的一个字符。例如：[^0-9]代表匹配一个不是数字的字符。


2. Bash中的其他特殊符号
''
""
`` 反引号
$()
#
$
\




------------------------------











